#!/bin/bash
# This script installs all Python packages listed in requirements.txt

# Exit immediately if a command exits with a non-zero status
set -e

# Install requirements
pip install -r requirements.txt 